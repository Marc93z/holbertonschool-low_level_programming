#include "main.h"
/**
 * _islower - check c is lower case
 * @c: (int) key
 *
 * Return: 1 if c is lowercase and 0 otherwise
 *
 */
int _isupper(int c)
{
        if (c >= 'A' && c <= 'Z')
        {
                return (1);
        }
        else
        {
                return (0);
     	}
}
